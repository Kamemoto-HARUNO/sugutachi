# AGENT運用ルール

このリポジトリで AI エージェントや開発者が作業するときは、以下のフローを基本ルールとして必ず守ること。

## 1. 直接 `main` で作業しない

- `main` では実装作業をしない
- 修正は必ず作業用ブランチを切って行う
- 既定の起点ブランチは `staging` とする
- ブランチ名は `feature/...` `fix/...` `chore/...` `docs/...` など内容がわかる形にする

## 2. 基本のリリースフロー

1. `staging` を最新化する
2. `staging` から作業ブランチを切る
3. ローカルで修正する
4. ローカルで必要な確認を行う
5. 作業ブランチから `staging` への PR を作る
6. `staging` へマージ後、開発環境 `dev.sugutachi.com` にデプロイして確認する
7. 問題なければ `staging` から `main` への PR を作る
8. `main` へマージ後、本番環境 `sugutachi.com` にデプロイして確認する

## 3. 作業前チェック

- いまいるブランチが `main` でないことを確認する
- 作業開始前に `git status` がきれいであることを確認する
- `staging` が古い場合は先に更新してからブランチを切る

## 4. ローカル確認

- 変更内容に応じて `php artisan test`、個別 Feature test、`npm run typecheck`、`npm run build` など必要な確認を行う
- migration を伴う場合はローカル DB にも適用して確認する
- ビルド成果物の反映漏れが起きないよう、フロントエンド変更時は必要に応じて `npm run build` まで行う

## 5. 禁止事項

- `main` に直接コミットしない
- `main` に直接 push しない
- 開発確認を飛ばして本番へ直接反映しない
- `staging` と `main` の差分を未確認のまま上書きしない

## 6. 例外

- 緊急対応が必要な場合でも、原則として `hotfix/...` ブランチを切って作業する
- 緊急で本番対応した場合は、反映後に同じ内容を `staging` に必ず戻す
- このルールを崩す必要がある場合は、ユーザーまたは管理者の明示的な合意を取る
